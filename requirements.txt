# LocalHelpBot — runtime requirements
#
# Core deps (always required)
chromadb>=1.0
requests>=2.32
pydantic>=2.6              # typed config schema (Slice 4)
rich>=13                   # colored logs + healthcheck (Slice 3 / 6)
cryptography>=42           # DPAPI/Fernet secrets (core/secrets.py)
tiktoken>=0.7              # RAG chunker token count
langchain-text-splitters>=0.3
langchain-community>=0.3

# Discord gateway (optional — only needed if you use the Discord bot)
discord.py>=2.7

# Document tools (read_pdf / write_pdf / read_docx / write_docx)
pypdf>=4.0
python-docx>=1.0
reportlab>=4.0

# Process tools (better list_processes / kill_process)
psutil>=5.9

# ─────────────────────────────────────────────────────────────────
# v4 additions
# ─────────────────────────────────────────────────────────────────

# Deep computer-access tools (Slice 2)
Pillow>=10                   # screenshot
pyperclip>=1.8               # clipboard read/write
# pywin32 is already a peer dep on Windows (list_windows tool)

# Optional web-extras (Slice 3) — graceful no-op if missing
# youtube-transcript-api>=0.6   # uncomment + install for the youtube_transcript tool

# Test suite (Slice 0.4)
pytest>=8
